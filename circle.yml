machine:
  pre:
    - curl -L https://sh.rustup.rs | sh -s -- -y
  environment:
    PATH: /usr/local/bin:${PATH}
compile:
  override:
    - pushd shell
    - cargo build
    - popd
test:
  override:
    - pushd shell
    - cargo build
    - popd
