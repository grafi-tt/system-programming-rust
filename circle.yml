machine:
  pre:
    - curl -L https://sh.rustup.rs | sh -s -- -y
  environment:
    PATH: ${HOME}/.cargo/bin:${PATH}
compile:
  override:
    - cd shell && cargo build
test:
  override:
    - cd shell && cargo build
