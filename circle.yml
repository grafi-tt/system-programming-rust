machine:
  pre:
    - curl -L https://sh.rustup.rs | sudo sh -s -- -y
compile:
  override:
    - pushd shell
    - cargo build
    - popd
test:
  override:
    - pushd shell
    - cargo build
    - popd
